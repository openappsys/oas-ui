# OAS-UI Roadmap

版本遵循 SemVer。v1.0.0 为首个正式发布。每个版本 = 一个可独立交付/可试用的里程碑。

## 版本规划

| 版本        | 内容                                                                                                                                                                                                                                                       | 状态      |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| **v0.1.0**  | 工程骨架：monorepo（pnpm）+ Vite library 构建 + Vitest + Playwright 基础设施 + CI 脚本 + `OASElement` 基类 + theme token（light/dark）+ 首个组件 Button 全链路跑通（含视觉/无障碍基线）                                                                    | ✅ 已完成 |
| **v0.2.0**  | 地基组件：icon（SVG 集+按需）、tag、badge、space、divider、link、typography（text/title/paragraph）；文档站骨架（Vitepress + 组件 demo 页）；构建补课（d.ts 产出、多入口 exports、tree-shaking 验证）；`OASElement` 增量渲染重构                           | ✅ 已完成 |
| **v0.3.0**  | 表单组 I：input、textarea、checkbox、radio、switch、slider、input-number、rate —— 受控/非受控双模式 + 原生元素优先                                                                                                                                         | ✅ 已完成 |
| **v0.4.0**  | 表单组 II：select（单选/多选）、auto-complete、cascader、tree-select、form（声明式校验 + aria-invalid 注入）                                                                                                                                               | ✅ 已完成 |
| **v0.5.0**  | 反馈组：message、notification（命令式 API）、modal + confirm、drawer、popconfirm、alert、progress、loading-bar、spin、skeleton、empty、result；overlay 管理器 + z-index token                                                                              | ✅ 已完成 |
| **v0.6.0**  | 浮层基础设施：定位引擎 + 焦点陷阱 + tooltip、popover、dropdown、menu、breadcrumb、tour、anchor、back-top、contextmenu、hover-card                                                                                                                          | ✅ 已完成 |
| **v0.7.0**  | 导航与布局：tabs、pagination、steps、segmented、affix、splitter、flex、page-header、float-button、layout、grid                                                                                                                                             | ✅ 已完成 |
| **v0.8.0**  | 数据展示重头：table（排序/分页/空态/loading/多选）、tree、card、avatar、image、collapse、descriptions、timeline、list、carousel                                                                                                                            | ✅ 已完成 |
| **v0.9.0**  | 主题与可访问性收官：多主题（light/dark/高对比）+ 自定义主题文档 + 全组件 axe 无障碍审计 + React/Vue 双宿主 playground                                                                                                                                      | ✅ 已完成 |
| **v0.10.0** | 国际化：locale registry（`@oas-ui/i18n`，`setLocale()` 全局切换）+ 语言包 zh-CN/en（tree-shakable）+ 全组件内置文案改造为 locale key + locale-completeness 类型约束测试 + Intl.* 数字/日期格式化规矩 + RTL 逻辑 CSS 属性规矩（全量 RTL 视觉审计挪 v1.x） | ✅ 已完成 |
| **v0.11.0** | 框架级容器（自 v1.6 前移）：config-provider（全局注入 locale/size/theme，收编 v0.10 的 registry）+ app（消息上下文容器，与 config-provider 配套）                                                                                                          | ✅ 已完成 |
| **v1.0.0**  | 正式发布：npm 发布、文档站上线、CHANGELOG、贡献指南、SSR 边界策略文档                                                                                                                                                                                      | ✅ 已完成 |

> v1.0 核心集约 68 件，覆盖通用组件库高频部分；长尾按下列 v1.x 版本推进至 100% 覆盖（约 115~120 件）。

## v1.x 规划（组件长尾，目标 100% 覆盖）

| 版本 | 内容 | 状态 |
| --- | --- | --- |
| **v1.1** 基础补充 | button-group、label、kbd、visually-hidden；增强：flex→wrap/stack、grid→simple-grid、tag→chip | ✅ 已完成 |
| **v1.2** 日期时间族 | date-picker、time-picker、calendar、countdown、statistic | ✅ 已完成 |
| **v1.3** 表单增强 | upload、transfer、mentions、color-picker、toggle-button、**pin-input**、**dynamic-input**、**dynamic-tags**、**editable**；增强：input→addon、textarea→autosize | ✅ 已完成 |
| **v1.4** 数据增强 | virtual-list、table 固定列/虚拟滚动、tree 虚拟化、image-preview、qrcode、watermark、**ellipsis**；增强：progress→circle | ✅ 已完成 |
| **v1.5** 命令与导航增强 | command（命令面板）、menubar、navigation-menu、toolbar、scroll-area、toggle-group、speed-dial、**toast**、**snackbar**、**backdrop** | ✅ 已完成 |
| **v1.6** 内容与展示长尾 | chart、code、log、marquee、number-animation、gradient-text、equation、aspect-ratio、masonry、comment | ✅ 已完成 |
| **v1.7** 框架级容器长尾 | theme-editor、bottom-navigation、sidebar、**container**（config-provider/app 已前移 v0.11.0） | ✅ 已完成 |
| **v1.8** 收尾 | combobox（可过滤单选组合框，区别于 select/auto-complete 定位）+ 文档站中英双语与完善（英文参考页与 demo 英文化、本地搜索、组件总览、CHANGELOG 页、图标墙、API 表自动化） | ✅ 已完成 |
| **v1.9** SSR/DSD + 发布冲刺 | 第一阶段 ✅：ssr 渲染器 + Node-safe 入口 + 白名单试点。第二阶段 ✅：真水合、闪动治理、数据通道（table/tree/select）、白名单 13 tag、grid 栅格表单（oas-form-item）、CHANGELOG 回填、首载优化（22ms）。第三阶段 ✅：尺寸档位扩展（size 五档 xs~xl，button/tag/switch/space/spin + 非法值回落告警）。第四阶段 ✅：DSD 彻底落地——白名单 123/124 tag（五批全量推进）、嵌套组件递归序列化、CDN 单文件 bundle（gzip ~116KB）、ssr.md 摘实验转正。框架集成插件 ✅：`@oas-ui/nuxt`（Nuxt 3 module，isCustomElement + theme 注入 + SSR helper）+ `@oas-ui/next`（RSC OasComponent + OasRegistry 客户端注册引导），SSR 开箱即用。已发布：npm（OIDC Trusted Publishing）+ 文档站上线（自定义域名） | ✅ 已完成 |

| **v1.9.1** | 工程性能（v1.9 PATCH）：e2e 大文件并行化——demo-coverage 10.4min→38s、code 8.3min→45s、visual 6.9min→46s，chromium 全量 ~15min→4.1min（CI 3-shard 每 shard 17min→~5min）；消灭固定等待改自动等待；CI webServer 跳过重复 docs build。另含文档站 GA 统计、GA pageerror 修复、Cloudflare 部署命令修复（全量 `pnpm build`）、dark 断言竞态修复、`OAStour`→`OASTour` 类名规范化 | ✅ 已完成 |
| **v2.0** 能力补齐第一批 | P0：upload 照片墙（picture-card/picture）+ 拖拽 + oas-exceed/oas-preview；date-picker shortcuts/disabled-date/multiple；select 自定义渲染（oas-option-render/oas-tag-render）+ 虚拟滚动；form inline 行内布局；table 行内编辑 + 吸顶行；modal 全屏 + 命令式确认（loading）；card 封面/操作区/hoverable/clickable；badge 缎带 ribbon；avatar 徽标 + fallback；tree 自定义节点 + 目录模式；image 懒加载。P1：transfer 搜索/单向/虚拟滚动；notification 进度条/可滚动；slider 输入联动/自定义滑块/reverse/range；calendar 自定义单元格/月年模式切换；message 分组/更新；tabs 动态增删 + 图标；steps 点状/导航模式；breadcrumb 折叠/省略 | ✅ 已完成 |
| **v2.1** basic 族组件增强 | button 增强（variant 形态/color/wave/auto-insert-space/circle/icon-position/href/plain/target/autofocus/wrap）+ button-group（pill 胶囊/分隔符/嵌套组/拆分按钮）+ icon（spin/rotate/flip/图标库注册/动画预设/duotone）+ floating 箭头（tooltip/popover/dropdown arrow/arrow-point-at-center/auto-adjust-overflow）+ tag-group（标签组：单选/多选选值组）+ tag 增强（预设色板、dot/processing 状态点、avatar 适配、hit/strong/multiline）+ badge 增强（standalone 独立徽标、color 全模式、offset 偏移、status 状态点、size 小尺寸、attention 吸引动画、corner 四角定位、overlap 圆形内收、ribbon-form 形态体系、premium、ribbon-size/direction/anchor）+ table 增强（size 密度档位 small/medium/large，CSS 变量开口可覆盖）+ space 增强（separator 分隔符+自定义插槽、justify 分布、reverse 反向、size 数组、fill/fill-ratio 填满、响应式断点简写 direction/size、行内嵌入 demo）+ **oas-compact**（相邻表单控件贴边合并边框组件：vertical/disabled/block）+ 展示型组件字号继承（11 组件）+ 官网首页 v2（hero oas-table 标志性 demo + 3 场景卡 + HTML 代码速览 + 真实 perf 数据 + CTA 收尾）+ button demo 自定义色 WCAG AA 达标 + **六件深挖收尾**：divider（variant 四线型/inset·middle 缩进/size 三档/strong/6 CSS 变量/vertical 撑满）、link（underline 三态/icon+icon-position/external 自动 target·rel/info 语义色/underline 变量开口）、typography（修饰六布尔/tag 换标签/depth/line-clamp/copy-text/ellipsis-suffix/actions 操作条）、kbd（variant 四形态/size 三档/color 统一协议）、label（error/disabled/colon/tooltip 组合演示/color）、visually-hidden（focusable 焦点显形）+ **code 深挖**（inline 行内代码/word-wrap/trim/size 四档/variant 四形态/color 统一协议）+ **无障碍体系**：WCAG 3 感知对比度门禁（自实现公式 + contrast-gate 工具）、-text 达标 token 体系（22 预设/语义）、color 属性统一协议（ui-spec §4.1）、dark 语义色达标（primary/danger 粉彩亮化）、a11y/qa-regression 断言大幅扩充、API 表自动化（api:scan/api:gen，35 条空描述补录）+ **集成反馈批次**（oas-md-ka 真实集成反馈）：slider/input-number 受控写回、modal 视口高度保护、tabs hover 反馈、间距中间档 token、集成 FAQ 页；menubar shortcut/多组 value/kind 未纳入（待排期） | ✅ 已完成 |
| **v2.1.1** | 文档补丁（组件 API 零变更）：8 个发布包 README 中英双语化（单文件 `[中文](#中文) | [English](#english)` 锚点切换，默认中文在上，顶部与 English 段前各一条切换栏）+ CDN 引用版本号 `@1`→`@2`（随主版本 2.x 解析最新 v2.1.x） | ✅ 已完成 |
| **v2.1.2** | 集成反馈第二批（菜单族）：menubar/menu 多组单选独立勾选（`value` 支持 JSON 对象按组 id 作用域，`type:"group"` 项 value 作组 id）、动作项 `kind="action"`（menuitem 无勾选态、不写回 value、emit 带 kind）、menubar `shortcut` 快捷键（kbd 视觉 + document keydown 自动绑定）、事件 detail 联合类型 api:scan 改进、e2e CI flaky 修复（demo-coverage 探针 upgrade 等待 + 事件缺失自动重试） | ✅ 已完成 |
| **v2.1.3** | Tabs 能力补齐（能力补齐）：disabled/size 五档/centered/justified/溢出滚动+箭头/more（滚动+视口外镜像下拉+搜索+点选平滑滚动）/panel-mode(keep/lazy/destroy)/activation/animated/before-change 拦截/editable 重命名/sortable 拖拽/嵌套/slot=label 自定义 + 二次补缺（滚轮滑动/新增滚到可见/+ 按钮固定/更多下拉搜索定位）+ 能力复核补齐（trigger:hover/allow-deactivation/stacked/icon-only/指示线定制/reserve-space 防抖/tab 即链接/hide-content 纯导航/items 数据驱动/scroll-position/PageUp-Down/add-close 图标 slot）+ 下划线 ::after 渲染修复 + 首页页脚 logo/底部空白/版权行间距 | ✅ 已完成 |
| **v2.1.4** | basic 族 12 组件复核补齐（能力并集）：button（icon-end 双侧/loading 保宽+文本+异步 auto/disabled-focusable/download/rel）+ button-group（spread/variant/round 透传）+ tag（close-icon/close-label/loading 异步关闭/checked-icon）+ badge（bordered 描边/徽标 icon/aria-live 播报/outline/size 三档）+ typography（align/weight/numeric/mark 变量开口）+ ellipsis（direction start/middle）+ link（download/size/loading）+ divider（垂直内容对齐/文字方向/垂直 inset/middle 缩进）+ kbd（语义键名映射）+ label（size/weight）+ space（弹簧占位 demo）+ button 链体积预算上调 | ✅ 已完成 |
| **v2.1.5** | menu/dropdown/contextmenu 能力补齐（能力并集）：menu（checkbox 多选项/danger/href 链接项/max-height/typeahead/mode=inline 就地展开/expanded 受控/accordion 手风琴/水平溢出收纳「···」+ 弹层选中反馈/close-on-select 选中收起策略）+ dropdown（trigger 多选/hover 防抖/placement 12 向/oas-open-change/disabled/hide-on-click/开合动画/close-on-scroll/offset）+ contextmenu（长按触发/show(x,y) 编程式/受控 open/右键别处关闭/close-on-scroll）+ 实测修复（inline hover-click 抵消/flex 并排/role 嵌套 axe 违规/水平浮层裁剪/「···」自收与弹层截断/inline 误收起） | ✅ 已完成 |
| **v2.2.3** | 导航与浮层族复核批：tooltip/popover 之外 9 件按组件深挖流程复核——tour 7 缺陷修复（keydown 重连/portal 插槽桥接/箭头指向/auto-reposition 实装/aria-modal/过渡对齐）+ 4 能力（gap 双轴/point-at-center/indicators/actions 插槽）；hover-card 滚动跟随默认开 + sticky 三档 + collision-boundary（含坐标系缺陷修复）；command 动画/aria-controls/append-to + keydown 重连修复；breadcrumb 子元素声明式通道试点（oas-breadcrumb-item/separator，双通道架构）+ collapse-click + ellipsis 裁剪修复；anchor oas-click 分离 + resize 重算 + block=nearest；menubar href 真链接 + 溢出收纳「···」（含 SSR 零宽误判三连修，水合漂移 38px 根治）+ indeterminate + start/end 插槽；navigation-menu 箭头跟随 bug 修复 + 碰撞翻转 + 面板内二级子导航 + 营销位插槽；back-top 滚动祖先自动探测 + 拖拽 + tooltip 读屏；toolbar 重连丢失修复 + 镜像 menuitemcheckbox + start/end 插槽 + 溢出收纳防收缩修复；缺陷固化回归 6 条。**实测第二批**：浮层箭头全家族统一 12px + merge 贴角描边三修（popover/hover-card）；tour 5 连修；icons arrow-up/down 方向修复 + generate 原子化；layout/sidebar/sider/table 模板缺陷 9 项（active 受控/drawer-open 观察/sider slot 判据/折叠联动/宽度变量/cell render Node/[hidden]）；select 下拉 fixed 定位逃出 overflow；tabs more offview 语义纠偏（部分滚出计入，空下拉消除）；menubar shortcut 契约（修饰键+键/单键 F1-F12 绑定）；sidebar items.group 分组标题；tabs tab-badge 颜色开口（--oas-tabs-badge-bg/-color + part=badge）；全量 3280 单测 + e2e 969 + 对比度门禁 0 违规 | ✅ 已完成 |
| **v2.2.4** | layout 域深挖批（实测第三批）：**tabs** 右键操作菜单 context-menu（新建/关闭/关闭其他/关闭左侧所有/关闭右侧所有/关闭全部——oas-add/oas-close 契约复用、菜单项中性「新建」宿主可 setLocale 覆盖、menu 模式键盘 roving）+ more 下拉键盘可达（无障碍缺陷）；**sidebar** 能力全量补齐（嵌套子菜单/折叠 tooltip/键盘导航/shortcut/badge/divider/loading 骨架屏/expand-on-hover/variant 三形态/side=right/actions/多 sidebar）+ 内置 resizable 拖拽调宽（rail/resize-min-max/键盘微调/oas-resize）+ hover 零对比修复 + 嵌套子菜单两视觉缺陷修复 + splitter 组合修复；**layout** 视口锁定 viewport 属性（100dvh 锁定、顶栏/底栏固定、侧栏/内容各自独立滚动）+ sider/sidebar 宽度契约（内嵌自动对齐：host-context 填满轨道/卸轨道 padding/折叠跟随）；**其他**：modal 滚动边缘指示、card 无 title 空占位修复、icons generate 并发竞态 + EPERM 重试加固、品牌标识定稿 <(w)> 紧凑版 + favicon 全套、docs 首页分隔光带补齐；工程坑记录（:host(:has()) Chromium scoped CSS 不生效 / ::slotted 特异性层级）；全量 3318 单测 + e2e 1370 + perf:size 六项 PASS | ✅ 已完成 |
| **v2.2.5** | 双通道推广 + sidebar 深挖批：**子元素声明式通道三批 12 组件**（导航系 menu/menubar/navigation-menu/anchor → 浮层族 dropdown/context-menu/command → 布局表单族 sidebar/bottom-navigation/select/toggle-group/toolbar-toggle；items 显式优先/子元素收敛同一路径/MutationObserver；virtual-list 豁免）；**sidebar 深挖批**：hide-toggle/accordion 手风琴/嵌套平滑动画（grid 0fr/1fr+visibility 联动+reduced-motion 降级）/嵌套机制无限级（3 级实证）/图标通道打通 registerIcon 正路+iconColor/--oas-sidebar-bg 背景开口/child-selected 激活后代指示 + 折叠态系列修复（父项死交互/徽标角标/hidden 失效根治/底色粘连/缩进收敛/引导线对比度）；**date-picker 浮层定位引擎**（fixed+computePosition 锚定+placement 12 向+碰撞翻转+range 宽面板，模板右缘被裁修复）；**radius scale 补全五档**（xs 2/xl 14）；**icons +4**（organization/tree/language/translate，translate 两轮字形修正）；**chart** 面积图 gradient 渐变填充 + smooth 曲线末点脱节修复；全量 3419 单测 + e2e 1372 + perf:size 六项 PASS | ✅ 已完成 |
| **v2.2.6** | sidebar 内置图标配 iconColor 生效修复（path 的 currentColor 替换）+ 图标着色 demo 补内置图标场景；全量 3423 单测 + e2e 1372 + perf:size 六项 PASS | ✅ 已完成 |
| **v2.2.7** | table 组件能力补齐（列显隐/列拖拽重排/列宽/多列排序/序号列/省略号/多级表头/内置分页/列过滤/合并单元格/子元素声明式声明列/单元格模板/自定义列头/编辑校验）+ core 增 ReactiveController 注入协议 + tabs 纵向 nav 底线修复 + table 列拖拽精确化/过滤触发器 focus-visible 修复；全量 3471 单测 + perf:size/perf:bench PASS + table 页 a11y/dark/visual/console/smoke 全 PASS | ✅ 已完成 |
| **v2.2.8** | form 图标 + oas-form collectFields 覆盖常用控件 + oas-layout side 属性（左/右/顶菜单）+ core escapeHtml/escapeText/escapeAttr 工具 + 统一 HTML 转义（5 组件）+ table 行点击忽略交互控件（内嵌 popconfirm 原生自驱动）+ popconfirm detail.source + avatar text 属性契约；全量 3482 单测 + typecheck/build/api:check 全绿 | ✅ 已完成 |
| **v2.2.9** | 布局组能力补齐三批（排布四原语：grid 行列距分离/断点/order/对齐/自宽列 + flex 补档/填满/断点 + container 流体/突破；交互容器：splitter 垂直/折叠/复位/延迟/手柄/像素/多面板 + scroll-area 编程滚动/阴影/贴底/到底/RTL；展示容器：masonry 响应式列数/行列距/fresh/column 指定列 + aspect-ratio 预定义 token/number）+ masonry items 数据驱动 + table summary-scope 合计范围（含聚合性能记忆化）+ docs 分组重构/首访语言适配/grid demo 色块梯度；修复 grid 单值 gap 被长hand清空（真实浏览器 CSSOM 顺序敏感）+ e2e 基建 locale 锁定与 demo-coverage 补全；全量 3589 单测 + e2e 1378 + typecheck/build/api:check 全绿 | ✅ 已完成 |
| **v2.3.0** | 框架级容器能力补齐：config-provider 四通道（config 组件级默认配置/direction RTL/z-index 起始值层序保持/disabled 全局禁用+双侧豁免）+ oas-app message/notification 全局配置与宿主注册栈式 + theme-editor 五件（颜色函数双通道/importJson+exportCss/preset 预设/数字滑块/折叠搜索）+ modal 命令式确认框（confirm/语义变体/异步 onOk 加载态/挂 app 容器）+ table 行内编辑退出修复；全量 3677 单测 + e2e 1380 + 对比度门禁 4558 采样 0 不达标 + typecheck/build/api:check 全绿 | ✅ 已完成 |
| **v2.3.1** | table 可编辑单元格可感知线索（hover 淡底色+cursor: text/铅笔图标 hover 显现/title 双击提示，sticky 不透底、编辑退出图标重挂，保持双击护文本选中）；全量 3682 单测 + e2e 1382 + typecheck/build/api:check 全绿 | ✅ 已完成 |
| **v2.4.0** | **源码目录统一到文档站语义七组**（breaking：floating 拆空归位 navigation/feedback/layout/framework、layout 6 件归 navigation、data 2 件归 layout，ESM 旧路径直接 breaking 不保留别名，exports 收敛四键）+ **CDN 族包多入口**（七族 IIFE 产物 dist/cdn/<族>.js 族包自包含 + cdn.js 全量保留，≤2 族引族包/≥3 族引全量）+ **title 双轨**（title 吸收 14 组件消除原生 tooltip + slot=title 富内容通道，ui-spec 吸收约定成文）+ **15 组件能力增强**（alert/notification/message/toast/drawer/snackbar/popconfirm/backdrop/spin/loading-bar/popover/tooltip/modal 三期/color-picker 两期）+ qa-regression 拆 55 spec（巨石串行→文件级并行）+ E2E_PORT 端口覆盖；修复：popconfirm 打开焦点从未生效 + demo-coverage wait 步从未执行（欠账清零 33→0）；全量 4970 单测 + e2e 1612 + typecheck/build/api:check/trace 全绿 | ✅ 已完成 |
| **v2.3.2** | 导航组七组件深挖收口 + **oas-stepper 步骤面板一体机**（新组件）：双组件同构 tabs 模式——oas-stepper（步骤头：steps/current 双向/linear 禁跳/clickable 默认 true/direction horizontal·vertical/size 五档）+ oas-stepper-panel（value 关联序号，仅 current 匹配可见，内容走默认插槽）；oas-change{index} 跳步事件（bubbles+composed）；键盘/ARIA：tablist+tab 语义、roving tabindex 左右/上下+Home/End、Enter/Space 激活（linear 禁跳）、tabpanel+aria-labelledby 关联；SSR template/hydrate 双路径（DSD 白名单后续批次）；demo 中英双版（基础/线性禁跳/竖向/面板联动/自定义步骤内容插槽） | ✅ 已完成 |


| **v2.2.2** | 导航与浮层族 11 组件能力补齐（能力并集全量补齐）：tooltip（placement 12 向/trigger 多选/延迟分离/富内容/Esc+describedby/interactive/动画/skip-delay/portal/color 变体/箭头形态）+ popover（hover 触发/width 同宽/初始焦点/关闭按钮/modal 化/碰撞细调）+ hover-card（浮层可悬停立身修复/富内容/箭头/role 修正）+ breadcrumb（icon/图标分隔符/真实链接/折叠可配/项下拉/键盘导航/JSON-LD SEO）+ anchor（滚动容器/落点偏移/嵌套/横向/滚动联动/墨水条/affix/oas-anchor-target）+ back-top（visibility-height 自动显隐/target/插槽/进度环/expand/reverse）+ tour（placement 12 向/滚动重定位/step 级覆盖/hints 信标/不再显示记忆/多页引导）+ command（图标/kbd 标注/loading/外部过滤/hotkey 可配/open-change/嵌套页面/最近使用/虚拟滚动/多选；豁免：内置模糊搜索并入打包勾选特性）+ menubar（checkbox/typeahead/open 受控/click 首开/汉堡收纳/竖排）+ navigation-menu（大面板网格/延迟开合/viewport 动画/indicator/backdrop；disclosure navigation 语义）+ toolbar（toggle/separator/input 新部件/orientation/溢出收纳/is-attached）+ 浮层定位引擎 12 向升级 + 实测修复（toolbar SSR 误判/popover aria-hidden 滞后/back-top 水合竞态） | ✅ 已完成 |

> 组件总数：v1.0 核心集约 68 件；v1.x 长尾推进至约 115~120 件，100% 覆盖。原 13 个未排期组件已全部分配：轻量基础组件（button-group/label/kbd/visually-hidden）提到最前的 v1.1（独立、无前置依赖、快速交付），其余按功能族归到 v1.3/v1.4/v1.5/v1.7，无遗漏。

## 版本号规则

- **MINOR（0.x.0）**：每个路线图功能块一个 MINOR 版本
- **PATCH（0.x.y）**：当前功能块内的 bug 修复
- 版本号同步位置：`package.json`（各包）+ 文档站侧栏版本号

## 质量门槛（每个版本发布前，对应 docs/engineering.md）

1. `pnpm test` 全绿（单测 + 行为测试）
2. `pnpm typecheck` 零错误
3. `pnpm build` 成功（含 d.ts 产出）
4. `pnpm test:e2e` Playwright 通过（交互 + 视觉基线 + axe 无障碍）
5. 该版本组件 demo 在文档站可用、React/Vue 宿主各一个页面跑通
6. PRD 版本段更新（含测试数）+ 本 ROADMAP 状态更新
7. 涉及 Shadow DOM 样式/组件的改动提交前必跑 `pnpm build`（vitest/jsdom 抓不到 scoped CSS 语法错误）

## 后续 backlog（按需立项）

- SSR / DSD：第一步（基类防御）与首版（SSR 渲染器 + 白名单试点）已落地；真水合、测量组件闪动治理、property-only 数据声明式通道、框架集成插件已并入 v1.9 第二阶段
- ~~表单布局（grid 栅格表单）~~ → 已并入 v1.9 第二阶段
- 组件 API 表格自动化：✅ 已完成（随 v1.8 落地；scan/harvest/gen + api:check 进 CI）
- 语言包扩展：v0.10.0 落地 zh-CN/en 后，按社区需求补 ja/ko/fr/de/es/ar 等
- 全量 RTL 视觉审计（v0.10.0 只立逻辑属性规矩）
